---
- name: Eliminar usuario basado en survey
  hosts: all
  become: true
  vars:
    username: "{{ gerardo }}"
  tasks:
    - name: Eliminar usuario si existe
      ansible.builtin.user:
        name: "{{ username }}"
        state: absent
        remove: yes 
