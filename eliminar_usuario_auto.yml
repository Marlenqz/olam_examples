---
- name: Eliminar usuario de forma automatica
  hosts: all
  become: true
  vars:
    username: "{{ gerardo }}"
  tasks:
    - name: Eliminar usuario si existe
      ansible.builtin.user:
        name: "{{ username }}"
        state: absent
        remove: yes 
