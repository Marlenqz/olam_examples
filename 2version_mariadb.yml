---
- name: Consultar versión de MariaDB sin módulos externos
  hosts: all
  gather_facts: no
  tasks:
    - name: Ejecutar SELECT VERSION()
      shell: "mysql -uroot -e 'SELECT VERSION();'"
      register: resultado_version

    - name: Mostrar versión
      debug:
        msg: "La versión de MariaDB en {{ inventory_hostname }} es: {{ resultado_version.stdout }}"
